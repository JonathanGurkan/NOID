fade_trigger = false;   // Trigger to start the fade
fade_alpha_max = 1;
fade_alpha = 0;
fade_length = 60; // Default fade duration in frames (modifiable by user)
fade_max = pi;   // Half a cosine wave (adjusted dynamically)
fade_time = 0;
fade_active = false;
fade_rate = 0;
